export interface PensionDetail {
    isPensionable: boolean;
    details: string;
    fromScheme: string;
    fromHusband: string;
    fromOther: string;
  }
  
export const pensionDetail = {
  isPensionable: Boolean,
  details: String,
  fromScheme: String,
  fromHusband: String,
  fromOther: String
};